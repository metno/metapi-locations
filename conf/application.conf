# This is the main configuration file for the application.
# ~~~~~

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
#
# This must be changed for production, but we recommend not changing it in this file.
#
# See http://www.playframework.com/documentation/latest/ApplicationSecret for more details.
play.crypto.secret = "AwYKlacQ=RrpLJ3b3>>AmFS[5XbAU9vi[gDhWxapMuvlxmSKsOo>FO[11r;B"

# The application languages
# ~~~~~
play.i18n.langs = [ "en" ]

# Application Loader
# ~~~~~
play.application.loader = modules.locations.LocationsApplicationLoader

# Router
# ~~~~~
play.http.router = locations.Routes

auth.active = false

# Database configuration
# ~~~~~
# Database connectivity for locations database
# You will need a local postgres database to test with data (as opposed to mock), since
# H2 does not support Postgis/Geo data.  
#db.locations.driver = org.postgresql.Driver
#db.locations.url = "jdbc:postgresql://localhost:5432/metapi"
#db.locations.username = metapi
#db.locations.password = "postgres"

# Swagger configuration
#play.modules.enabled += "play.modules.swagger.SwaggerModule"

# Development Configuration
# ~~~~~
mail.override.enabled = true
mail.override.address = "root@localhost"

# You should add a personal configuration file called development.conf in your conf directory
# with settings that are useful in dev mode; e.g., auth.active=false
# To use a local Postgres database set up on localhost (with admin user metapi/postgres)
# add the following:
#db.locations.driver = org.postgresql.Driver
#db.locations.url = "jdbc:postgresql://localhost:5432/metapi"
#db.locations.username = metapi
#db.locations.password = "postgres"
include "./development.conf"
