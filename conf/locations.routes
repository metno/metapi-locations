# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# MET API
GET     /v0.:format                 controllers.LocationsController.getLocations(name: Option[String], format: String)
