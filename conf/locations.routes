# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# MET API
GET     /v0.:format                 controllers.LocationsController.getLocations(names: Option[String], geometry: Option[String], fields: Option[String], format: String)
